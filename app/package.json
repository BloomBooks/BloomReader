{
  "name": "bloom-player-dependencies",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "//": "Developers need to `yarn upgrade bloom-player` to get the latest, because of yarn.lock. (TeamCity does this automatically.)",
    "bloom-player": "2.11.0"
  },
  "scripts": {},
  "volta": {
    "node": "20.15.1",
    "yarn": "1.22.22"
  }
}
